<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context="com.universidadcauca.movil.fragmentos.principal"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:background="@drawable/fondo_principal"
    android:padding="40dp"
    >


    <TextView
        android:id="@+id/titulo"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Arriendos"
        android:textColor="#ffffffff"
        android:textSize="@dimen/texto_principal"
        android:gravity="center"
        android:layout_margin="@dimen/margen_1"
        />


    <ImageView
        android:id="@+id/icono"
        android:layout_width="match_parent"
        android:layout_height="@dimen/imagen_principal"
        android:src="@drawable/icon"
        android:layout_below="@+id/titulo"
        />

    <Button
        android:id="@+id/boton_face"
        android:textColor="#ffffff"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:layout_below="@+id/icono"
        android:layout_margin="@dimen/margen_2"
        android:text="Tap to singin with Facebook"
        android:drawableLeft="@drawable/icon_face"
        android:background="#00000000"
        />


</RelativeLayout>
